/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Para deployment en subdirectorio si es necesario
  // basePath: '/nextjs',
  output: 'standalone',
}

module.exports = nextConfig
