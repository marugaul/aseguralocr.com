-- ============================================
-- TABLA DE PRUEBA - Sistema Auto-Ejecutor
-- ============================================
-- Esta es una prueba del sistema automático
-- Si ves esta tabla en phpMyAdmin = FUNCIONA
-- ============================================

CREATE TABLE IF NOT EXISTS test_auto_executor (
    id INT AUTO_INCREMENT PRIMARY KEY,
    mensaje VARCHAR(255) NOT NULL,
    ejecutado_en TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    sistema VARCHAR(50) DEFAULT 'Auto-Executor v1.0'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT INTO test_auto_executor (mensaje)
VALUES ('¡Sistema funcionando! El cron ejecutó este SQL correctamente.');

SELECT
    '============================================' AS '';
SELECT
    '✅ TABLA DE PRUEBA CREADA EXITOSAMENTE' AS resultado;
SELECT
    '============================================' AS '';
SELECT * FROM test_auto_executor;
